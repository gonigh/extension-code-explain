export enum GptModelEnum {
    HX = 'hx',
    OPENAI = 'openai'
}

export enum MessageRoleEnum {

    SYSTEM = 'system',

    USER = 'user',

    ASSISTANT = 'assistant'
}

export enum SceneEnum {
    CHAT = 'chat',
    CODE_EXPLAIN = 'code_explain',
    TEST_UNIT = 'test_unit',
}